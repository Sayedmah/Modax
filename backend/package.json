{
  "name": "modax-ai-backend",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs",
    "test": "node --test ../tests/*.test.mjs"
  },
  "dependencies": {
    "jose": "^6.1.0"
  }
}
